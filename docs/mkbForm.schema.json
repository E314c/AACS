{
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "u_mask": {
                "type": "number",
                "description": "The U mask of the MediaKeyBlock entry"
            },
            "uv_number": {
                "type": "number",
                "description": "The UV number of the MediaKeyBlock entry"
            },
            "mediaKeyData": {
                "type": "string",
                "description": "A hex representation of the encrypted media key. In the spec this is stored in a separate list with an index matching the subsetDifference list, but is included here for consiseness"
            }
        }
    }
}